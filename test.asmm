STRING saludo = "hola a todos";

STRING primer_mensaje = "inserta un numero";

MODULE main {
  PRINT saludo;
  PRINT primer_mensaje;
  MOV INU ACC;
  MOV ACC mmem;
  MOV INU ACC;
  ADD mmem;
  MOV ACC OUTP;
  HALT;
}

MEM mmem;
